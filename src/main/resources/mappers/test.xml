<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whlll.mapper.Itest">
    <insert id="add">
        insert into manager(m_username,m_password,m_invite) values(#{Test.mUsername},#{Test.mPassword},#{Test.mInvite});
    </insert>
</mapper>